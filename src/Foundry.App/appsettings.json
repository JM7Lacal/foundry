{
  "Assistant": {
    "_comment": "Provider: stub | claude-code | anthropic | ollama. Cambiar aca (o en appsettings.Local.json) es lo unico que hace falta para cambiar de modelo.",
    "Provider": "stub",

    "_secrets": "NUNCA pongas una API key aca (este archivo va al repo). Usala en appsettings.Local.json (ignorado por git) o en la variable de entorno Assistant__ApiKey.",

    "_claudeCode": "Provider=claude-code usa el CLI 'claude -p' y tu sesion local de Claude Code; no necesita API key. Busca 'claude' en el PATH y, si no, la instalacion nativa en %APPDATA%\\Claude\\claude-code. 'Command' fuerza una ruta.",
    "Command": "",

    "_anthropic": "Provider=anthropic requiere ApiKey (via appsettings.Local.json o Assistant__ApiKey). Modelo por defecto: claude-haiku-4-5-20251001.",
    "ApiKey": "",

    "_ollama": "Provider=ollama necesita Ollama en localhost:11434. Modelo por defecto: qwen2.5.",

    "Model": ""
  }
}
